import { Syne } from "next/font/google"

// Distinctive display font — gives the "Órbita" product its own identity
export const syne = Syne({
  subsets: ["latin"],
  weight: ["600", "700", "800"],
  display: "swap",
})
